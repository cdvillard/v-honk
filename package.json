{
  "name": "v-honk",
  "version": "1.0.1",
  "description": "#TheHonkening arrives to Vue",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf lib && babel src -d lib"
  },
  "author": "Charles Villard",
  "license": "MIT",
  "dependencies": {
    "vue": "^2.6.10"
  },
  "private": false,
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "rimraf": "^3.0.0"
  },
  "peerDependencies": {}
}
